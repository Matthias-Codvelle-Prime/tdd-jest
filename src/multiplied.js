// Implement the multiplied function here
function multiplied(a, b) {
    let result = a * b;
    if (result === 6) {
        return result;
    } else {
        return "Ah bah bravo hein ! (╯°□°）╯︵ le resultat n'est pas 6 ! ";
    }
}
module.exports = multiplied;

// couvertures es différentes catégories :
//% Stmts : déclaration 
//% Branch : branches if else ..
//% Funcs : fonctions
//% Lines : lignes que l'on parcours
//% Uncovered Line #s : numéro de la ligne correspondante au fichier.